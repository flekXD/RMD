# Лабораторна робота №3: Гра-Клікер з Жестами (React Native Expo)

Це мобільний додаток, розроблений в рамках Лабораторної роботи №3. Мета роботи - закріпити знання з теми взаємодії користувача з додатком у React Native за допомогою бібліотеки `react-native-gesture-handler`.

Додаток являє собою просту гру-клікер з інтерактивним об'єктом та системою завдань, що використовують різні типи жестів.

## 🎯 Тема

Використання жестів у React Native за допомогою `react-native-gesture-handler`.

## ✨ Основні Функції

**1. Головний екран:**
   * **Лічильник кліків:** Відображає поточний рахунок гравця.
   * **Інтерактивний об'єкт:** Візуальний елемент (коло), що реагує на жести користувача.
   * **Обробка жестів:**
        * **Коротке натискання (`TapGestureHandler`):** +1 очко.
        * **Подвійний клік (`TapGestureHandler`):** +2 очки.
        * **Довге натискання (`LongPressGestureHandler`):** +10 бонусних очок за утримання протягом ~1.5 секунд.
        * **Перетягування (`PanGestureHandler`):** Дозволяє вільно переміщувати об'єкт по екрану.
        * **Свайп (`FlingGestureHandler`):** Свайп вліво або вправо додає випадкову кількість очок (1-10).
        * **Масштабування (`PinchGestureHandler`):** Збільшення/зменшення об'єкта пальцями додає бонусні очки (+5).

**2. Екран Завдань:**
   * **Список завдань:** Відображає перелік цілей, які користувач має досягти (зробити певну кількість кліків, свайпів, набрати очки тощо).
   * **Відстеження прогресу:** Показує поточний прогрес виконання кожного завдання та відмічає виконані.
   * **Реалізація:** Використовує `FlatList` для відображення списку завдань.

**3. Навігація:**
   * **Використання `@react-navigation/native`:** Реалізовано перехід між Головним екраном та екраном Завдань.
   * **Тип навігації:** Використано Нижню Таб-Навігацію (`@react-navigation/bottom-tabs`) для зручного перемикання.

## 🛠️ Технології

* **Expo:** Платформа для розробки універсальних React-додатків.
* **React Native:** Фреймворк для створення нативних мобільних додатків за допомогою JavaScript та React.
* **react-native-gesture-handler:** Бібліотека для обробки торкань та жестів.
* **react-native-reanimated:** Бібліотека для створення плавних анімацій та ефективної роботи з жестами (працює в UI-потоці).
* **@react-navigation/native:** Основа для навігації в React Native додатках.
* **@react-navigation/bottom-tabs:** Реалізація навігації за допомогою вкладок знизу екрану.
* **React Context API:** Для управління глобальним станом гри (рахунок, прогрес завдань).

## ⚙️ Передумови

Перед запуском проекту переконайтеся, що у вас встановлено:

* [Node.js](https://nodejs.org/) (LTS версія рекомендована)
* `npm` або `yarn`
* [Expo CLI](https://docs.expo.dev/get-started/installation/) (глобально або через `npx`)
* Додаток **Expo Go** на вашому фізичному пристрої (iOS або Android) або налаштований емулятор/симулятор.

## 🚀 Встановлення та Запуск

1.  **Клонуйте репозиторій:**
    ```bash
    git clone <URL_вашого_репозиторію>
    cd <назва_папки_проекту>
    ```
2.  **Встановіть залежності:**
    ```bash
    npm install
    # або
    yarn install
    ```
3.  **Налаштуйте `babel.config.js`:**
    Переконайтесь, що файл `babel.config.js` містить плагін `react-native-reanimated/plugin` **останнім** у списку плагінів:
    ```javascript
    module.exports = function(api) {
      api.cache(true);
      return {
        presets: ['babel-preset-expo'],
        plugins: [
          // Інші плагіни можуть бути тут
          'react-native-reanimated/plugin', // Обов'язково останній!
        ],
      };
    };
    ```
    Якщо ви щойно додали цей плагін, очистіть кеш перед наступним запуском: `npx expo start -c`

4.  **Запустіть додаток:**
    ```bash
    npx expo start
    # або
    npm start
    # або
    yarn start
    ```
5.  **Відкрийте додаток:**
    * **На фізичному пристрої:** Відскануйте QR-код, що з'явиться у терміналі, за допомогою камери або додатку Expo Go.
    * **На емуляторі/симуляторі:** Натисніть `a` для запуску на Android емуляторі або `i` для запуску на iOS симуляторі (якщо вони встановлені та запущені).

## 📁 Структура Проекту